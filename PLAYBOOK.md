# Build Day Playbook — 從 drone91 / rtc_rocket / astronauts91 / ast91 提煉

用途:今晚 90 分鐘、從零長出一個單檔 HTML 模擬。題目換了也適用。
來源都是真的發生過的事(`drone91/docs/DESIGN-THINKING.md`、各 repo 鐵則表、工作紀律記憶)。
**給 workflow 的 agent 讀的就是「建造規則」和「審查規則」兩節。**

---

## 0. 一句話

> 產品是迴圈,不是那個畫面。

評審看到的是畫面,但贏的是「拉一根滑桿,整個解跟著動,而且自己證明算對」。
每個決定先問:它讓「改參數 → 看結果」變快還是變慢?

---

## 建造規則(寫程式的 agent 照做)

1. **參數住資料,不住程式碼。** 檔案最上面一個 `PARAMS` 物件,所有可調量、物理常數、
   預設值都在那裡,帶單位註解。滑桿只是改 `PARAMS`。程式碼裡只留演算法。
   *(drone91 教訓:87 個外型參數有 45 個藏在 .py 裡,卡上那格只是印出來的標籤,改了什麼都不會發生。)*

2. **兩個迴圈分開。** 物理用固定步長 `DT` 積分,畫面用 `requestAnimationFrame`;
   每幀跑 N 個物理步(N 由時間倍率決定)。物理絕不依賴幀率。
   *(120 Hz 螢幕上跑出不同答案 = 綁在一起了。)*

3. **全程 SI 單位,只在顯示那一層換算。** bar、mm、kN 只出現在格式化函式裡。

4. **靜默少做比報錯更危險。** 出現 NaN、負壓力、解不收斂 → 畫面上亮紅字講哪個量壞了,
   不准悄悄 clamp 成一個好看的數字繼續跑。

5. **渲染熱路徑的禁忌。** 每幀不准:重建 DOM、改 innerHTML 整張表、配置新陣列、
   重畫靜態背景。靜態層畫一次到離屏 canvas;表格只更新變動的 textContent。
   軌跡/曲線用環形緩衝區。
   *(ast91 web viewer 教訓:變卡的原因從來不是三角形數量,是最後一次改動加進每幀路徑的東西。)*

6. **沒量的欄寫「—」,不猜。** 還沒燒完就沒有總衝量的最終值;還沒到頂點就沒有 apogee。
   顯示「計時中 / measuring」(官方 Aurelith 就是這樣做)。

---

## 審查規則(驗證的 agent 照做;也是副駕盯螢幕時的清單)

1. **自我一致的檢查必然給滿分。**
   驗證表的兩欄如果是同一個式子移項,差距恆為 0.00% —— 那是裝飾不是驗證,
   懂行的評審一眼看穿。每一列必須是:**數值積分 vs 封閉解**、**流量積分 vs 幾何狀態**、
   **功 vs 能量** 這種真正獨立的兩條路。
   *(欽揚案教訓:用 libredwg 驗 libredwg 自己寫的檔,一路自評 94 分,真實 72 分。)*

2. **每道閘門要答得出:它什麼時候會判 FAIL?**
   測法:故意把 `DT` 調大 50 倍,或把一個常數改錯 —— 對應那列**必須變紅**。
   不會紅的列砍掉或重做。

3. **偏差有方向,就不是誤差,是構造錯。**
   隨機散布 → 容差。全部同號 / 恆等於某常數 / 隨時間單調漂 → 公式或單位錯,
   放寬門檻吸不掉。不准用調常數的方式讓列變綠。

4. **先懷疑量具,再懷疑被量的東西。** 列不綠時先查「量測那一側」怎麼算的
   (取樣時機、起點、單位),再去動物理。

5. **摘要會藏錯,看逐筆。** 曲線看起來很順不算數;把某一瞬間的全部狀態變數印出來,
   手算一列對帳。

6. **數字過了不等於做完。** 真的打開檔案、真的按每一顆按鈕、真的拉每一根滑桿到兩端。
   console 零錯誤才算開得起來。

7. **排他宣稱與外推是造假入口。** 上台不講「完全準確」「跟真實引擎一樣」。
   講得出口的是:「模擬跟它自己的封閉解差 0.0X%」「參數是我們引擎的真實工作點」。
   *(FLOWVIS 教訓:不是 CFD 就別說是 CFD,弱點自己先講。)*

---

## 節奏規則(人照做)

- **粗估先行。** 18:00 先要一個會動的醜版本,不要等完美版本。
- **同一處修到第三次 = 重寫訊號。** 同一個 bug 來回三輪還不好,
  不要第四輪 —— 叫它把那個子系統依第一性原理整段重寫。
- **一次只問一題。** 每輪只給一個修改要求。
- **可逆決策自己走預設值。** 配色、版面、字體 —— 不討論,直接選,不好再換。
- **宣稱「做不到」之前先花十分鐘測。**
- **改上游一律重跑下游。** 改了物理之後,驗證表、曲線、apogee、講稿裡的數字全部重看一遍。
  講稿裡的數字最後才填,而且從畫面上抄,不從記憶抄。
- **19:10 凍結。**

---

## 對抗式覆核(workflow 的形狀)

有效過的做法:多個互斥角度各自試圖推翻產出,再由獨立複驗官剔除假警報,
外加一隻**盲點批評員**(只問「大家都沒看的那一維是什麼」)。

今晚縮成 4 個角度就夠:
1. **物理** — 公式、單位、因次、極限情形(流量→0、喉徑→極大)
2. **驗證表獨立性** — 每列是否真的兩條路、會不會 FAIL
3. **首開體驗** — console 錯誤、60fps、按鈕全按、滑桿拉到兩端
4. **across-the-room** — 三公尺外看得懂嗎、2 分鐘講得完嗎
